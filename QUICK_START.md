# 🚀 دليل البدء السريع - Quick Start Guide

اتبع هذه الخطوات السريعة للبدء مع تطبيق إدارة المخزن والمبيعات في دقائق معدودة!

---

## ⚡ 5 دقائق للبدء

### الخطوة 1: تثبيت Flutter (إذا لم تقم به)
```bash
# تحميل Flutter من:
# https://flutter.dev/docs/get-started/install

# التحقق من التثبيت:
flutter --version
```

### الخطوة 2: إنشاء المشروع
```bash
# إنشاء مشروع جديد
flutter create inventory_app
cd inventory_app
```

### الخطوة 3: نسخ الملفات
1. انسخ جميع ملفات `lib/` من التطبيق
2. استبدل محتوى `pubspec.yaml` بالملف المرفق
3. تأكد من هيكل المجلدات:
```
inventory_app/
├── lib/
│   ├── main.dart
│   ├── models/
│   ├── screens/
│   ├── services/
│   └── providers/
├── pubspec.yaml
└── README.md
```

### الخطوة 4: تثبيت المتعلقات
```bash
flutter pub get
```

### الخطوة 5: تشغيل التطبيق
```bash
flutter run
```

✅ **تم! التطبيق جاهز للاستخدام**

---

## 📱 أول استخدام

### المرة الأولى:
1. **افتح الشاشة الأولى**: شاشة إدارة المخزن
2. **أضف منتج تجريبي**:
   - اسم: "أرز"
   - كنية: "AR"
   - كمية: 100
   - سعر: 25 ريال

3. **انتقل لشاشة البيع**:
   - اختر المنتج
   - أدخل كمية: 10
   - أكّد البيع

4. **شاهد التقارير**:
   - سيظهر البيع مع الإجمالي

---

## 🎯 الميزات الأساسية (في دقيقة)

| الميزة | كيفية الاستخدام |
|--------|-----------------|
| ➕ إضافة منتج | زر + في شاشة المخزن |
| ✏️ تعديل منتج | اضغط على الثلاث نقاط |
| 🗑️ حذف منتج | اضغط الثلاث نقاط → حذف |
| 🛒 بيع منتج | انتقل لشاشة البيع واختر المنتج |
| 📊 عرض التقارير | الشاشة الثالثة - يعرض الشهر الحالي |
| 📅 بحث بفترة | اختر من التاريخ إلى التاريخ |

---

## 🔧 الإعدادات الأساسية

### تغيير اللون الأساسي (Optional):
في `lib/main.dart`:
```dart
theme: ThemeData(
  primarySwatch: Colors.blue,  // غيّر إلى اللون المفضل
  useMaterial3: true,
  // ...
)
```

### تغيير اسم التطبيق:
في `android/app/build.gradle`:
```gradle
defaultConfig {
    applicationId "com.example.inventory_app"
    minSdkVersion 21
    targetSdkVersion 34
}
```

### تغيير أيقونة التطبيق:
استخدم الأداة `flutter_launcher_icons`:
```bash
flutter pub add flutter_launcher_icons
```

---

## 📊 سيناريو عملي كامل

### اليوم الأول: إعداد المخزن

**الصباح:**
1. أضف 5 منتجات:
   - أرز عادي (100 كيلو، 25 ريال)
   - أرز فاخر (50 كيلو، 35 ريال)
   - سكر أبيض (75 كيلو، 8 ريال)
   - دقيق (100 كيلو، 6 ريال)
   - زيت نباتي (30 لتر، 15 ريال)

**المتجر:**
1. بع أرز عادي: 20 كيلو بـ 25 ريال
2. بع سكر: 30 كيلو بـ 8 ريال
3. بع دقيق: 10 كيلو بـ 7 ريال

**نهاية اليوم:**
- اذهب للتقارير
- سترى الإجمالي: 20×25 + 30×8 + 10×7 = 990 ريال
- عدد المبيعات: 3

---

## 🆘 المشاكل الشائعة وحلولها

### ❌ المشكلة: "flutter command not found"
**✅ الحل:**
```bash
# أضف Flutter إلى PATH
export PATH="$PATH:/path/to/flutter/bin"

# تحقق:
flutter --version
```

### ❌ المشكلة: "No connected devices"
**✅ الحل:**
```bash
# قائمة الأجهزة المتاحة:
flutter devices

# تشغيل المحاكاة (Android):
emulator -avd pixel_4

# أو استخدم جهاز حقيقي
```

### ❌ المشكلة: "SDK is not found"
**✅ الحل:**
```bash
flutter config --android-sdk /path/to/android/sdk
flutter doctor
```

### ❌ المشكلة: "Database not creating"
**✅ الحل:**
```bash
# احذف التطبيق وأعد تشغيله:
flutter clean
flutter pub get
flutter run
```

### ❌ المشكلة: الواجهة لا تحدّث البيانات
**✅ الحل:**
```dart
// تأكد من استخدام Provider بشكل صحيح:
context.read<InventoryProvider>().loadProducts();
```

---

## 🎓 تعلم أثناء الاستخدام

### نصيحة 1: جرّب الميزات
- أضف 10 منتجات
- سجّل 20 عملية بيع
- غيّر الأسعار والكميات
- لاحظ كيف تتحدّث البيانات

### نصيحة 2: اختبر الحالات الخاصة
- حاول بيع كمية أكثر من المتاح (سيظهر خطأ)
- احذف منتج (سيختفي من القائمة)
- غيّر السعر وشاهد التأثير على التقارير

### نصيحة 3: استكشف الكود
- اقرأ التعليقات في الملفات
- افهم كيف تعمل `Provider`
- تعلم عمليات قاعدة البيانات

---

## 🚀 التطوير الإضافي (اختياري)

### إضافة ميزة جديدة:

#### مثال: إضافة عملة مختلفة
في `lib/models/product.dart`:
```dart
class Product {
  // ... الحقول الموجودة ...
  final String currency; // إضافة جديدة
}
```

#### مثال: تغيير اللغة
في `lib/main.dart`:
```dart
MaterialApp(
  locale: Locale('ar', 'SA'), // تعيين اللغة
  supportedLocales: [
    Locale('ar', 'SA'),
    Locale('en', 'US'),
  ],
)
```

---

## 📱 التطبيق على أجهزة مختلفة

### تشغيل على Android:
```bash
flutter run -v
```

### تشغيل على iOS (Mac فقط):
```bash
flutter run -d iphone
```

### تشغيل على ويب (تجريبي):
```bash
flutter run -d chrome
```

### بناء نسخة للإطلاق:
```bash
# Android APK
flutter build apk

# Android App Bundle
flutter build appbundle

# iOS
flutter build ios
```

---

## 📞 الحصول على المساعدة

### الموارد المفيدة:
- 📖 [Flutter Documentation](https://flutter.dev/docs)
- 🔧 [Dart Documentation](https://dart.dev/guides)
- 💬 [Flutter Gitter Chat](https://gitter.im/flutter/flutter)
- 🐛 [Stack Overflow - Flutter Tag](https://stackoverflow.com/questions/tagged/flutter)

### أسئلة شائعة:

**س: هل يمكن استخدام التطبيق على عدة أجهزة؟**
ج: حالياً لا، كل جهاز له قاعدة بيانات مستقلة. يمكن إضافة مزامنة في المستقبل.

**س: هل يمكن تصدير البيانات؟**
ج: يمكن باستخدام مكتبات مثل `excel`, `pdf`, إلخ.

**س: هل التطبيق آمن؟**
ج: البيانات محلية 100%، لا يوجد اتصال بالإنترنت أو سرفرات خارجية.

**س: هل يمكن تشفير قاعدة البيانات؟**
ج: نعم، استخدم `sqflite_sqlcipher` بدلاً من `sqflite`.

---

## ✅ قائمة التحقق (Checklist)

قبل البدء بالاستخدام الفعلي:

- [ ] تثبيت Flutter بنجاح
- [ ] إنشاء المشروع
- [ ] نسخ جميع الملفات
- [ ] تشغيل `flutter pub get`
- [ ] تشغيل التطبيق بدون أخطاء
- [ ] إضافة منتج تجريبي
- [ ] تسجيل عملية بيع
- [ ] عرض التقارير
- [ ] محاولة تعديل وحذف

---

## 🎉 ماذا بعد؟

بعد إتقان الأساسيات:

1. **اقرأ التوثيق الكاملة** (`README.md`)
2. **استكشف الكود** (`API_DOCUMENTATION.md`)
3. **أضف ميزات جديدة** حسب احتياجاتك
4. **شارك الملاحظات** والتحسينات

---

## 💪 التطوير المستقبلي

### ميزات يمكن إضافتها:
1. **🔐 نسخ احتياطية تلقائية**
2. **📤 تصدير التقارير (PDF, Excel)**
3. **📱 دعم متعدد اللغات**
4. **🌙 الوضع الليلي**
5. **📊 رسوم بيانية متقدمة**
6. **🔔 تنبيهات المخزون المنخفض**

---

**استمتع باستخدام التطبيق! 🎊**

لأي استفسارات أو مشاكل، تفضل بالاطلاع على `README.md` أو `API_DOCUMENTATION.md`
